import test from 'node:test';import assert from 'node:assert/strict';import {createGeometryStore} from '../../armada-three-geometry.js';
test('mesh storage preserves typed geometry and partial damage edits without a WebGL context',()=>{const {gl,geometry}=createGeometryStore(),v=gl.createBuffer(),i=gl.createBuffer(),vao=gl.createVertexArray();gl.bindVertexArray(vao);gl.bindBuffer(gl.ARRAY_BUFFER,v);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([1,2,3,4,5,6]));gl.vertexAttribPointer(0,3,gl.FLOAT,false,12,0);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,i);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,new Uint32Array([0,1,0]));const before=geometry(vao);gl.bufferSubData(gl.ELEMENT_ARRAY_BUFFER,4,new Uint32Array([0]));assert.deepEqual([...geometry(vao).i],[0,0,0]);assert.ok(geometry(vao).version>before.version);gl.deleteVertexArray(vao);assert.equal(geometry(vao),null);});
