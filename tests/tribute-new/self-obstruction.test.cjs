const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');
const {loadBattle}=require('./headless-battle.cjs');
function scene(){const b=loadBattle(),html=fs.readFileSync('armada-war-tribute-new.html','utf8');b.run(html.slice(html.indexOf('function forgeFireHull('),html.indexOf('function forgeMountSites(')));b.run(`var vessel={race:6,slen:100,x:0,y:0,z:0,yaw:0,pitch:0,roll:0,exL:50,exY:20,exZ:20};var v=[],indices=[];function wall(y0,y1){const n=v.length/3;v.push(20,y0,-10,20,y1,-10,20,y1,10,20,y0,10);indices.push(n,n+1,n+2,n,n+2,n+3);}wall(-20,-5);wall(5,20);vessel.fireHull=forgeFireHull({v,i:indices});`);return b;}
test('superstructure blocks a shot but the actual gap and clear deck remain usable',()=>{const b=scene();assert.equal(b.run('ownHullClear(vessel,[0,10,0],[100,10,0],0)'),false);assert.equal(b.run('ownHullClear(vessel,[0,0,0],[100,0,0],0)'),true);assert.equal(b.run('ownHullClear(vessel,[0,25,0],[100,25,0],0)'),true);});
test('self-obstruction follows ship yaw, pitch and roll',()=>{const b=scene();assert.equal(b.run(`vessel.yaw=1.2;vessel.pitch=.35;vessel.roll=.7;ownHullClear(vessel,gunWorld(vessel,[0,10,0],2),gunWorld(vessel,[100,10,0],2),2)`),false);});
test('surface contact permits outward fire but a second surface blocks it',()=>{const b=scene();assert.equal(b.run('ownHullClear(vessel,[20,10,0],[100,10,0],0)'),true);b.run('wall(5,20);for(let k=12;k<v.length;k+=3)v[k]+=20;vessel.fireHull=forgeFireHull({v,i:indices})');assert.equal(b.run('ownHullClear(vessel,[20,10,0],[100,10,0],0)'),false);});
test('manual persistent beam is removed when own hull moves into its path',()=>{const b=scene();assert.ok(b.run(`(()=>{vessel.id=0;vessel.vao={};ships=[vessel];beams=[{manual:true,coherent:true,from:0,muz:[0,10,0],b:[100,10,0],t0:0}];pinBeams(0);return beams[0].t0===-Infinity;})()`));});
